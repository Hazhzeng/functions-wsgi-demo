const initialState = {
  title: undefined,
  tags: undefined,
  text: undefined,
  update: undefined,
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};